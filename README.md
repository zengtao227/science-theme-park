# Science Theme Park // 科学逻辑乐园

## 🚀 项目愿景 (Vision)
一个面向瑞士初高中生 (Sek I/II) 的硬核科学逻辑模拟器。我们不只是在做题，而是在构建一个遵循物理规律和数学逻辑的虚拟游乐园。在这里，每一个数学公式都是游乐设施的“操作规程”。

---

## 🏗️ 开发架构 (Applied Architecture)

### 1. 知识点分层 (Learning Layers)
每个模块必须严格遵循从“抽象”到“具象”的闭环：
- **[BASIC/CORE]**：武器库演练。掌握公式的基础变换。
- **[ADVANCED/ELITE]**：压力测试。处理复杂参数和多步逻辑。
- **[MISSION: APPLIED]** 🌟 (**核心新增**)：
    - **生活化场景**：基于瑞士生活、职业工作（如建筑、物流、金融）的真实建模。
    - **概念图标 (Concept Icons)**：每个章节配有直观几何图形，辅助视觉识别与记忆。
    - **情境化 UI**：在 SVG/Canvas 画布上呈现具象背景（如房屋、影子、火车）。
    - **建模挑战**：学生必须从业务语言中提取数学参数。

### 2. 多语言策略 (i18n Strategy)
- **德语优先 (DE)**：瑞士第一教学语言，所有题目首发德语。
- **英语辅助 (EN)**：作为编程语言和国际化的桥梁。
- **中文延后 (CN)**：作为架构支持，但在开发阶段优先保证 DE/EN 质量。
- **顺序**：UI 顶部始终显示 `DE -> EN -> CN`。

---

## 🏗️ 技术标准 (Technical Standards - Chamber V2)

项目已全面转向模块化架构，所有新模块及重构必须遵循：

1.  **架构解耦**：
    - **逻辑层**：统一使用 `src/hooks/useQuestManager.ts` 钩子处理状态，禁止在页面内手写 nonce/validate 逻辑。
    - **UI 层**：页面必须由 `src/components/layout/ChamberLayout.tsx` 包裹，确保全站视觉一致性。
2.  **视觉体验**：
    - 每个模块必须配备动态渲染组件（SVG/Canvas），位于 `src/components/chamber/`。
    - 严守“黑金/霓虹”实验室美学，使用 `ConceptIcon` 作为视觉索引。
3.  **i18n**：
    - 所有文案必须由 `src/lib/i18n.ts` 驱动，页面内禁止硬编码文本。

---

## 🗺️ 模块路线图 (Module Roadmap)

根据瑞士 **Lehrplan 21** 大纲，当前已实现的模块逻辑：

| 模块代号 | 主题 | 核心知识点 | 生活化应用场景 (Mission) |
| :--- | :--- | :--- | :--- |
| **S1.01** | 面积与体积 | 多边形面积、立体体积 | 建筑材料计算 |
| **S1.02** | 数据与概率 | 统计图表、概率基础 | 瑞士彩票概率分析 |
| **S2.01** | 二项式工厂 | $(a+b)^2, (a-b)^2$ | 庭院扩建规划 (Garden Layout) |
| **S2.02** | 勾股与平方根 | $a^2+b^2=c^2$, 空间距离 | 太阳能板安装角度/空间测距 |
| **S2.03** | 线性函数 | $y=ax+b$, 斜率截距 | 苏黎世不同计费模式的成本博弈 |
| **S2.04** | 相似与缩放 | 比例缩放、相似三角形 | 阴影测量法 (钟楼高度计算) |
| **S2.05** | 幂与根 | 指数运算、根式化简 | 复利计算与增长模型 |
| **S2.06** | 线性方程组 | 二元一次方程组 | 物流配送优化 |
| **S3.01** | 二次方程 | 求根公式、因式分解 | 足球/滑雪起跳点与射程预测 |
| **G1.01** | 微积分初探 | 导数、瞬时变化率 | SBB 火车进站时的瞬时速度计算 |

---

## 🛠️ 协作说明 (AI Routing)
本项目采用多模型路由开发：
- **Gemini 3**：项目构建与文件管理中心。
- **NVIDIA GLM-4.7**：后端数学逻辑与 R3F 物理精算。
- **NVIDIA MiniMax-2.1**：德意地文案与 UI 文本对齐。

---

## � 任务看板 & 未来规划 (Status & Roadmap)

### ✅ 已完成工作 (Milestones)
- **[2026.02.03] 仓库瘦身与规范**：
  - 建立了严格的 `.gitignore` 机制，将临时扫描件 (`.png`, `.pdf`) 与本地 MCP 配置彻底分离。
  - 确认了“源码 Main 仓库”与“产物 Release 仓库”的分离原则。
- **[2026.02.02] 核心模块交互强化**：
  - `S2.02`: 勾股与平方根太阳能板建模。
  - `S2.04`: 相似三角形钟楼测距 logic 完成。
  - `i18n`: 德语/英语/中文多语言自动化排序重构。

### 🚀 近期计划 (Next Steps)
1. **[UI/UX] 具象场景美化**：
   - 为 `S2.04` 添加动态阴影和太阳高度角控制条。
   - 使用玻璃拟态 (Glassmorphism) 重构实验室控制面板。
2. **[Logic] 瑞士生活场景建模**：
   - 优化 `S2.02` (勾股与平方根)：结合 Office Lens PDF 中的高频考题（阶梯路径计算、心算专项）。
   - 开发 `S1.02` (概率论)：基于瑞士彩票 (Swiss Lotto) 的数学概率模拟。
   - 开发 `S1.01` (几何)：苏黎世标志性建筑的体积计算。
3. **[CI/CD] 自动化 Release 流程**：
   - 编写专门的 `./release.sh` 脚本，实现“代码一键编译 -> 生成 APK/ZIP -> 自动推送到独立 `release` 仓库”。

---

## �📥 运行指南
```bash
npm install
npm run dev
```
访问：`http://localhost:3000`

---

## 📮 强制工作流 (Mandatory Workflow)

**1. [严禁积压] 严禁修改完代码不推送 GitHub。凡是代码逻辑有变动，修改完成后必须立刻执行：**
```bash
git add -A
git commit -m "feat/fix: 简要描述"
git push
```
**2. [编译检查] 推送前必须确保本地 `npm run build` 通过。**
