# GuideRoom setup note

This file was created only to record that the GitHub connector is scoped to `zengtao227/science-theme-park` for issue operations. GuideRoom issues should be created manually in `zengtao227/guideroom` or through a connector installation that is explicitly scoped to that repository.
